#include <stdlib.h>
#include <stdio.h>

int main (){
  printf ("inserici due numeri:");
  float a;
  float b;
  scanf("%f",&a);
  scanf("%f",&b);
  printf ("la somma dei due numeri è %f \n",a+b);
  return 0;
}
